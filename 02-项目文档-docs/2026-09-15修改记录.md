# 2026-09-15 修改记录

## 本次目标

为“7 天股票交易实验”准备 GitHub Actions 云端运行，同时确保任何真实 Binance 密钥都不会写入仓库、运行日志或 artifact。当前仍严格限定为 paper trading，真实交易未授权。

## 已完成修改

### GitHub Actions

- 新增 `.github/workflows/paper-trading-observation.yml`。
- 支持 GitHub 手动触发；手动触发使用 `--manual`。
- 按 2026 年 9 月 Central 夏令时配置每天六次 UTC cron。
- Python 运行器仍会根据 `03-定时任务-routines/schedule.json` 二次检查实验日期和允许窗口。
- 工作流权限限制为 `contents: read`，不向仓库自动提交运行结果。
- 同一时间只允许一个观察任务运行，最长运行十分钟。
- 运行结束上传白名单内的脱敏状态、journal 和 evidence，保留十四天。

### 密钥保护

- GitHub 工作流不保存任何真实密钥，只引用 `BINANCE_API_KEY`、`BINANCE_API_SECRET` 和可选的 `BINANCE_ENV` Secrets。
- 需要用户填写的文档位置全部保留为空白占位符。
- 云端运行时临时生成 `10-API密钥-仅本地-local-secrets/binance-api.env`，权限由 `umask 077` 限制，并在结束后删除。
- artifact 白名单不包含 `.env`、SQLite 数据库、锁文件或编辑器交换文件。
- `.gitignore` 已排除真实 `10-API密钥-仅本地-local-secrets/binance-api.env`、所有普通 `.env`、`05-交易记录-data/*.sqlite3*`、`04-运行状态-state/run.lock`、运行输出和系统临时文件。
- 已把本地真实 key/secret 与全部 Git 暂存文本做精确比对，结果为零命中。
- Binance API Key 曾在截图中暴露，建议实验稳定后重新生成只读密钥；不要把新密钥发到聊天窗口。

### 日程修复

- `due_slot()` 现在收集所有处于宽限期的候选任务，并选择距离当前时间最近的任务。
- 修复 14:45 精确触发时被 14:15 任务的 30 分钟宽限期抢先匹配的问题。
- 中英文 schedule 均新增 `activation_status: github_actions_prepared_not_deployed`。

### 文档同步

- 新增 `02-项目文档-docs/GITHUB-ACTIONS-RUNBOOK.zh-CN.md`，说明 Secrets、定时表、首次运行和固定 IP 限制。
- 更新 `README.md` 当前状态，替换旧的 20 USDT、认证失败和旧日程描述。
- 更新本交接说明顶部的最新状态，同时保留旧检查结果作为审计历史。

## 验收结果

- Python 单元测试：16 项全部通过。
- 日程检查：7 个交易日乘 6 个时段，共 42 个唯一槽位。
- GitHub Actions YAML：本地解析通过。
- `06-程序脚本-scripts/run_observation.py --dry-run`：正常返回计划槽位数量。
- 暂存安全检查：真实 env、SQLite、运行锁和交换文件均处于忽略状态。

## GitHub 部署结果

- 已重新登录 GitHub，当前账号为 `yomislight`。
- 已创建私有仓库 `yomislight/7-day-stock-paper-trading` 并推送 `main`。
- 首次提交：`2847b29 Initialize secure paper trading experiment`。
- 已通过 GitHub CLI 设置 `BINANCE_API_KEY`、`BINANCE_API_SECRET`、`BINANCE_ENV` 三个 Actions Secrets，过程没有打印值。
- 已手动触发工作流，运行编号 `34930058749`。
- 密钥验证、临时配置、安全删除和 artifact 上传步骤均完成。
- Binance 公开 `ping` 与时间接口返回 HTTP 451，观察步骤按安全设计以退出码 2 失败。
- HTTP 451 表明 GitHub 托管 Runner 所在地区访问受限，不是密钥错误。
- GitHub 日志中的两个 Binance 凭据均显示为 `***`，未发现密钥泄漏。
- 已将 GitHub 官方 Actions 升级到当前 v7，并移除 artifact 的无效参数。

## 当前尚未完成

- GitHub 托管 Runner 无法访问 Binance.com；需要允许地区的固定出口 VPS 或自托管 Runner。
- 当前 `06-程序脚本-scripts/run_observation.py` 只能做只读验证和留档，不能自动生成完整 paper 交易决策、模拟成交或更新盈亏。
- Telegram 手机推送为可选配置，目前没有确认已填写 `TELEGRAM_BOT_TOKEN` 和 `TELEGRAM_CHAT_ID`。

## 下一步

1. 准备一台允许访问 Binance.com、具有固定出口 IP 的 VPS 或自托管 Runner。
2. 把工作流的 `runs-on` 改为对应自托管标签并重新验证。
3. 实现并测试本地 paper 决策、模拟成交、费用、止损和盈亏更新模块后，才能称为自主 paper trading 系统。

## 安全结论

本次修改没有把 Binance 密钥写入提交、日志或 artifact，没有真实交易，也没有修改 Binance 权限。密钥已经以 GitHub Actions Secrets 的加密形式上传。GitHub Actions 当前状态是“私有仓库已部署并可触发，但 GitHub 托管 Runner 被 Binance HTTP 451 阻止”。
